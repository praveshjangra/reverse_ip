apiVersion: v1
kind: Service
metadata:
  name: reverse-ip-app
spec:
  selector:
    app: reverse-ip-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: {{ .Values.service.type }}

